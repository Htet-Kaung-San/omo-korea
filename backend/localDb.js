/**
 * Local JSON mock DB has been retired.
 * Configure real SUPABASE_URL / SUPABASE_KEY in backend/.env instead.
 */
throw new Error(
  "localDb.js is disabled. Hey! PNU uses Supabase only — set SUPABASE_URL and SUPABASE_KEY.",
);
